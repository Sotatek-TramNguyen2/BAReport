# BA-kit Community For Claude Code

Read:

- `core/contract.yaml`
- `core/contract-behavior.md`
- the selected skill under `skills/`

Supported workflow:

`input -> intake -> backbone -> stories -> srs-lite`

Unsupported:

- modules
- collaboration
- packaging
- publishing
- client delivery

When a request is out of scope, point to `docs/comparison.md` and `docs/upgrade.md`.
